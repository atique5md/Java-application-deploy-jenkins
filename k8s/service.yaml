apiVersion: v1
kind: Service

metadata:
  name: java-service
  namespace: java-app

spec:
  selector:
    app: java-app

  type: NodePort

  ports:
  - port: 80
    targetPort: 8080
    nodePort: 30080
